name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      # Sin .env en el runner, DEBUG caería a False y SECURE_SSL_REDIRECT
      # respondería 301 a todas las peticiones http del cliente de tests.
      DEBUG: "True"
    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v4

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Chequeo de configuración de Django
        run: python manage.py check

      - name: Recolectar archivos estáticos
        run: python manage.py collectstatic --noinput

      - name: Verificar que no hay migraciones pendientes
        run: python manage.py makemigrations --check --dry-run

      - name: Correr tests
        run: python manage.py test --verbosity 2
